<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.motion.widget.MotionLayout
        android:id="@+id/constraintLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutDescription="@xml/activity_presentation_xml_constraintlayout_scene"
        tools:context=".ui.presentation.PresentationActivity">

        <ImageView
            android:id="@+id/presentationImgView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dimen_200"
            android:scaleType="centerCrop"
            android:src="@drawable/unipi_presentation_img"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="contentDescription" />

        <TextView
            android:id="@+id/presentationTxtView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:justificationMode="inter_word"
            android:padding="@dimen/dimen_16"
            android:text="@string/quick_presentation_screen_content"
            android:textColor="@color/default_text_color"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/presentationImgView" />

    </androidx.constraintlayout.motion.widget.MotionLayout>
</layout>